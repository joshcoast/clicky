import React from "react";

const Header = () => (
  <header className="header">
    <h2 className="main-header">Click on an image to earn points, but don't click on any more than once!</h2>
  </header>
);

export default Header;


